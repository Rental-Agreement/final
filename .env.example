# Frontend environment variables
# Copy to .env (do not commit real secrets)

# Supabase (already present in .env in repo; keep using your project values)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# No API keys needed for Neighborhood Guide!
# Using free OpenStreetMap APIs (Nominatim + Overpass)
# 100% free, no registration, no limits for reasonable use
